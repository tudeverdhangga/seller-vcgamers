import Card from "@mui/material/Card";
import { styled } from "@mui/material/styles";

export default styled(Card)(() => ({
  boxShadow: "0 0 0",
  borderRadius: "10px",
  padding: "24px",
  marginTop: "16px",
  marginBottom: "16px",
}));
